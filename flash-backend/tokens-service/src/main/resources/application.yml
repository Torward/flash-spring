server:
  port: 8090

spring:
  application:
    name: tokens-service
  datasource:
    url: jdbc:postgresql://localhost:5432/flashdb
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

logging:
  level:
    ru.lomov.flash.tokens: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

# Prohibition notice: NO Firebase dependencies allowed
# This service implements Firebase-compatible FCM token management without Firebase SDK
